---
name: Provider research / tester report
about: Report live testing results (or research findings) for a non-OpenAI provider
title: "Provider research: <PROVIDER>"
labels: ["provider-support"]
assignees: []
---
<!--
Only OpenAI is tested end to end with real accounts today. Every other
provider is source-derived and needs testers. See docs/roadmap.md's
"Non-OpenAI live verification wanted" section and README.md's provider
matrix for current status.

If you have not already, run the copy-paste research prompt in
https://github.com/leinardi/opencode-swap/blob/main/docs/provider-research-prompt.md
inside OpenCode and paste its generated report below unchanged. It is
designed to gather source evidence and sanitized live observations without
ever exposing a credential.
-->

> ⚠️ **Never paste credentials.** No access tokens, refresh tokens, API keys,
> emails, account IDs, or `auth.json` contents — sanitized field
> names/types/presence only, exactly as the research prompt produces.

## Provider

* Provider ID (as it appears as a top-level key in `auth.json`): `?`
* Auth method (API key, OAuth, well-known URL, plugin-supplied): `?`
* OpenCode version: `?`
* `opencode-swap` version: `?`

## Commands run

<!-- e.g. `opencode-swap add <provider> <name>`, `opencode-swap list --usage`, `opencode-swap doctor` -->

```text
$
```

## Sanitized results

<!-- What worked, what didn't, any error text (scrubbed of secrets). -->

*

## Research prompt report (if used)

<!-- Paste the report generated by docs/provider-research-prompt.md here, unchanged. -->

```markdown

```
